import { AnalyticsProvider } from '../provider';
export default function createTagManagerProvider(dataLayer?: any[]): AnalyticsProvider;
export declare const joinName: (parts: string | string[]) => string;
