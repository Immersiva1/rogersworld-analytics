{
  "name": "rogersworld-analytics",
  "files": [
    "dist"
  ],
  "main": "./dist/analytics.umd.js",
  "module": "./dist/analytics.es.js",
  "exports": {
    ".": {
      "import": "./dist/analytics.es.js",
      "require": "./dist/analytics.umd.js"
    }
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build --config vite-lib.config.js && mv dist dist-lib && vite build && mv dist-lib/* dist/ && rm -rf dist-lib",
    "serve": "vite preview"
  },
  "devDependencies": {
    "prettier": "^2.3.2",
    "typescript": "^4.3.5",
    "vite": "^2.5.0"
  }
}
